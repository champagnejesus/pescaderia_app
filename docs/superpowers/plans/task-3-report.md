# Task 3 Report

- **Status:** DONE
- **Changes Made:**
  - `web/src/components/layout/TopBar.tsx`: Replaced content with Minimal Pro design, added `rightAction` prop, used `abyssal-*` tokens, sticky header, and 150ms transitions.
  - `web/src/components/layout/BottomNav.tsx`: Replaced content with Minimal Pro design, updated icons/labels, used `useRouter` instead of `Link`, and applied `abyssal-*` tokens.
- **Concerns:** None